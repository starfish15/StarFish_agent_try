---
name: tool_policy
description: "工具调用策略：只有在必要时才调用，并按 JSON 协议输出"
uses_tools: true
always_on: true
---
## 目标
当且仅当需要外部信息或确定性计算时，才调用工具。

## 规则
- 不需要工具时，直接自然语言回复。
- 需要工具时，只输出一段 JSON：
  {"tool": "工具名", "arguments": {"参数名": "参数值"}}
- 工具结果会以“工具执行结果：{...}”返回；拿到结果后再用自然语言回复。
